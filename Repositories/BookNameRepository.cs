using API.Entities;
using API.Interfaces;

namespace API.Repositories
{
    //public class BookNameRepository : IBookFilterRepository
    ////{
    ////    private readonly string? _bookName;
    ////    public BookNameRepository(string? bookName)
    ////    {
    ////        _bookName = bookName;
    ////    }

    ////    public IQueryable<Book> Filtering(IQueryable<Book> books)
    ////    {
    ////        return string.IsNullOrEmpty(_bookName) ? books : 
    ////            books.Where(b =>b.BookName.Contains(_bookName));
    ////    }
    //}
}
